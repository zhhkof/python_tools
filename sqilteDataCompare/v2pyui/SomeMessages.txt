1、对比sqlite数据库中表数据的差异（默认表和表结构一致）。

2、按全字段匹配，将db2中的记录与db1中的对比，若找不到完全匹配的记录则算作差异数据。

3、对比结果保存在工具目录下的Compare_result.txt，每次执行会覆盖前次结果。

4、按照要求，目前在结果文件中只列出“在db2中存在，而db1中不存在”的这类差异记录。

5、简单小工具，未作过多的异常输入校验，使用时请按要求格式输入文件和表名。

6、db2比db1多的表名会在结果中提示，若指定的表不存在于db2中，则会记录异常并退出对比。